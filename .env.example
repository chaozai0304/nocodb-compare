# Server
PORT=5175
# 如需允许前端跨域（携带 Cookie），建议显式设置：
# CORS_ORIGIN=http://localhost:5173

# 存储位置（建议不要放在仓库内）
# CONFIG_FILE=/data/config.json
# AUTH_FILE=/data/auth.json

# Session
# 建议生产环境设置一个强随机值
SESSION_SECRET=change-me

# 初始账号（仅在首次启动且 auth 文件不存在时生效）
INIT_USERNAME=admin
INIT_PASSWORD=ChangeMe123!
